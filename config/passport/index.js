const prismaService = require("../../data-access");

const userRepository = require("../../data-access/user");

const passportJWTs = require("../passport/passport-jwt");

module.exports = (passport) => {
    
};
